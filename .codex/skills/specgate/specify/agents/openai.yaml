interface:
  display_name: "SpecGate: specify"
  short_description: "Create or update the feature specification from a natural language feature description. (SpecGate workflow)"
  default_prompt: "Run the specify workflow from the SpecGate skill in the active feature context."
  brand_color: "#0EA5E9"
policy:
  allow_implicit_invocation: true
