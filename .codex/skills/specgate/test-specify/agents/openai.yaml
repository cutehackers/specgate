interface:
  display_name: "SpecGate: test-specify"
  short_description: "Execute the SpecGate test planning workflow and generate test-spec.md as the single test execution source. (SpecGate workflow)"
  default_prompt: "Run the test-specify workflow from the SpecGate skill in the active feature context."
  brand_color: "#0EA5E9"
policy:
  allow_implicit_invocation: true
