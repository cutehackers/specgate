interface:
  display_name: "SpecGate: checklist"
  short_description: "Generate a custom checklist for the current feature based on user requirements. (SpecGate workflow)"
  default_prompt: "Run the checklist workflow from the SpecGate skill in the active feature context."
  brand_color: "#0EA5E9"
policy:
  allow_implicit_invocation: true
